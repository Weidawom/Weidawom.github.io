name: chengxiang_app
description: "丞相 - 成长伙伴App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.4

dependencies:
  flutter:
    sdk: flutter

  # 自动更新所需依赖
  http: ^1.1.0
  package_info_plus: ^8.0.0
  flutter_downloader: ^1.11.6
  permission_handler: ^11.1.0
  open_file: ^3.4.1
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
